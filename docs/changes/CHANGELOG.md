# 变更日志

仅记录二开修改，用于未来与上游合并时快速定位差异。每次 AI 完成的实质性代码变更，在此文件追加一行。

| 日期 | 说明 | 涉及文件 | 与上游差异 | 合并指引 |
|------|------|---------|-----------|---------|
| 2026-06-18 | 启动时默认使用 default 主题 | `theme.go`（Frontend 默认值 `"classic"`→`"default"`）、`constants.go`（themeValue 初始化 `"classic"`→`"default"`） | 二开变更 | 保留 |
